# GoReleaser 設定
# 参考: https://goreleaser.com/customization/

version: 2

project_name: gyuma

before:
  hooks:
    - go mod tidy

builds:
  - id: gyuma
    main: ./cmd/gyuma
    binary: gyuma
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}

archives:
  - id: default
    format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

brews:
  - name: gyuma
    repository:
      owner: goqoo-on-kintone
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    directory: Formula
    homepage: "https://github.com/goqoo-on-kintone/gyuma"
    description: "OAuth 2.0 CLI tool for kintone"
    license: "MIT"
    install: |
      bin.install "gyuma"
    test: |
      system "#{bin}/gyuma", "-h"
